# Edooqoo — LLMs Reference

> AI-powered ecosystem for English tutors. 1-on-1 adult education.

## Key Architecture Decisions (April 2026)

### Meeting Links
- Permanent per-student links via `generateFallbackMeetingLink()` (Jitsi) or `gcal-sync` action `create_permanent_room` (real Google Meet)
- `isAutoGeneratedLink()` detects auto vs custom links
- Legacy per-lesson Meet toggle disabled when permanent links active
- `MeetingLinkField` component: Default/Custom toggle, external link button, empty-save support

### Calendar Stability
- `needs_review` auto-update guards: `student_id IS NOT NULL` filter + error handler
- Zombie slots (booked + no student) cleaned via migration
- Confirm/Reject uses inline comment (no sub-dialogs) — `showInlineComment` + `inlineComment` state

### Student Hub
- Lesson list: upcoming ascending, past descending
- Auto-scroll container to Today on load (150ms delay, container-only)

## Edge Functions
- `gcal-sync`: Actions: `upsert`, `delete`, `cancel`, `create_permanent_room`
- `student-gcal-sync`: Student calendar sync with token refresh
- `send-calendar-notification-email`: Booking/rejection/confirmation emails with optional teacher comments

## File Map
- `src/utils/meetingLinkUtils.ts` — Meeting link generation utilities
- `src/hooks/useCalendarSlots.tsx` — Calendar data + realtime + auto needs_review
- `src/components/calendar/SlotDetailModal.tsx` — Slot editing, confirm/reject with inline comment
- `src/components/calendar/StudentBookingsSection.tsx` — Student lesson list with auto-scroll
- `src/pages/StudentPage.tsx` — Student profile, MeetingLinkField component
- `src/pages/CalendarSettingsPage.tsx` — Calendar settings, meeting link toggles
