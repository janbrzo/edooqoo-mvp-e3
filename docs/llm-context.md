# Edooqoo LLM Context — AI Agent Reference

## Meeting Links — GCal-Only Model (v3, April 2026)
### Problem
1. Jitsi fallback (`generateFallbackMeetingLink`) produced identical links across students (hash collision).
2. `meet.google.com/lookup/` URLs are not real room links — they expire/redirect.
3. Frontend called `gcal-sync create_permanent_room` but edge function was blocked by slot fetch before action check (`slotId` was actually `studentId` → 404).
4. `studentId` was not destructured from request body; `req.clone().json()` after consumed body silently failed.
5. Global `calendar_settings.default_meeting_link` leaked as shared fallback even with per-student model active.

### Edooqoo Solution
- **No Jitsi, no lookup, no deterministic hashes.** Auto-generated = only real Google Meet via GCal API.
- Teacher must have active Google Calendar connection for auto-create to work.
- If no GCal → only manual custom link, no fake "default."
- `calendar_student_settings` stores: `generated_meeting_link` (auto), `meeting_link_mode` ('default'|'custom'), `default_meeting_link` (canonical effective link for backward compat).
- Student-facing flows: `slot.meeting_link` > `student_settings.default_meeting_link` > (no global fallback when per-student active).

### Technical Mechanics
- `supabase/functions/gcal-sync/index.ts`: **Critical fix (v3)**: `studentId` added to top-level destructuring. `create_permanent_room` action handled BEFORE slot fetch (early return). Removed broken `req.clone().json()`. Action flow: parse body → get token → if `create_permanent_room` handle and return → else fetch slot → handle `delete`/`cancel`/`upsert`.
- `create_permanent_room` accepts `teacherId + studentId` (no `slotId` needed), checks for existing `generated_meeting_link`, creates ghost event → extracts `hangoutLink` → deletes event → saves to `calendar_student_settings`.
- `src/pages/StudentPage.tsx` `MeetingLinkField`: reads `meeting_link_mode` from DB (no URL heuristics). Default mode triggers edge function if no generated link exists.
- `src/hooks/useStudents.tsx`: on new student + auto-create ON + GCal connected → calls edge function (no local link generation).
- `src/pages/CalendarSettingsPage.tsx`: batch generation calls edge function per student; toggle disabled when no GCal.
- `supabase/functions/get-student-hub-data/index.ts`: returns `studentMeetingLink || null` (no global fallback).
- `src/utils/meetingLinkUtils.ts`: only `isLegacyAutoGeneratedLink()` for backward compat detection.

### RAG Keywords
meeting link, Google Meet, permanent room, per-student link, video call, Jitsi removal, auto-create, calendar student settings, generated_meeting_link, meeting_link_mode, create_permanent_room, ghost event, hangoutLink, gcal-sync, 404 slot not found, studentId destructuring

---

## Confirm/Reject Inline Comments
### Problem
Sub-dialog for Confirm/Reject didn't open properly (portal conflict with DraggableDialog). Teacher couldn't add comments.

### Edooqoo Solution
Removed sub-dialogs entirely. Added inline checkbox "Add comment" + AutoResizeTextarea directly in the footer of `SlotDetailModal`. Confirm/Reject buttons trigger `handleConfirm`/`handleReject` immediately. Comment from `inlineComment` state is included in email and slot log.

### Technical Mechanics
- `src/components/calendar/SlotDetailModal.tsx`: States `showInlineComment`, `inlineComment` replace old `showConfirmDialog`/`showRejectDialog`/`confirmComment`/`rejectComment`
- Comment passed as `confirmationComment` or `rejectionReason` to `send-calendar-notification-email`

### RAG Keywords
confirm booking, reject booking, teacher comment, inline comment, booking decision, pending approval

---

## Calendar Stability (400 Errors / Freeze)
### Problem
"Zombie" slots (status=booked, student_id=NULL) caused 400 errors during `needs_review` auto-update. Fire-and-forget `.then(() => {})` masked errors, causing infinite re-render loops.

### Edooqoo Solution
1. Added `student_id` guard: `needs_review` auto-update only targets slots where `s.student_id` is truthy.
2. Added error handler: `.then(() => {}, (err) => console.error(...))` instead of fire-and-forget.
3. SQL migration cleaned existing zombie slots: `UPDATE calendar_slots SET status='available' WHERE status='booked' AND student_id IS NULL`.

### Technical Mechanics
- `src/hooks/useCalendarSlots.tsx` lines 103-122: Filter + error handling
- Migration: One-time data cleanup

### RAG Keywords
calendar freeze, 400 error, zombie slot, needs review, batch update, calendar stability

---

## Student Hub Lesson Sorting & Auto-Scroll (v2)
### Problem
Original sort: upcoming ascending then past descending. User expected newest-first (descending). Reschedule button visible but non-functional (missing props).

### Edooqoo Solution
- Unified descending sort (newest first). Auto-scroll to Today still works via `data-date` attribute scan (reversed logic for descending order).
- `StudentHubLessons.tsx` now wires `onRescheduleStart` and `rescheduleBookingId` props to `StudentBookingsSection`. Reschedule mode highlights available slots in blue; clicking a slot calls edge function `get-student-bookings` with `action: 'reschedule'`.

### Technical Mechanics
- `StudentBookingsSection.tsx`: single `result.sort()` descending by `${slot_date}${start_time}`. `scrollToToday()` finds first element with `date <= todayStr`.
- `StudentHubLessons.tsx`: `rescheduleBookingId` state, `handleReschedule()` invokes edge function, `handleSlotClick()` routes to reschedule or booking dialog.

### RAG Keywords
lesson list, student hub, today scroll, auto scroll, lesson sorting, booking list, reschedule, descending sort

---

## Compact SlotDetailModal Layout
### Problem
Modal too tall for 754px viewport, required scrolling.

### Edooqoo Solution
Combined rows: Date+Start+End+Duration in `grid-cols-4`, Worksheet+Discount in `grid-cols-[1fr_80px]`, Notes+Meeting Link in `grid-cols-2`. Reduced spacing from `space-y-3` to `space-y-2`, inputs from `h-9` to `h-8`. ~25% height savings.

### Technical Mechanics
- `src/components/calendar/SlotDetailModal.tsx`: `max-h-[85vh] overflow-y-auto` on content.

### RAG Keywords
slot detail modal, compact layout, lesson modal, modal height

---

## Bulk Actions (replacing Bulk Delete)
### Problem
"Bulk Delete" only allowed deleting available slots. No batch confirm/reject/complete/no-show.

### Edooqoo Solution
Renamed to "Bulk Actions". Selection restricted to one slot type at a time (`selectionType` state). Dynamic action buttons appear based on selected type: Delete (available), Confirm/Reject (pending), Complete/No Show (booked, needs_review).

### Technical Mechanics
- `src/pages/CalendarPage.tsx`: `getSlotSelectionType()` classifies slots. `selectionType` locks on first selection, resets when empty. `handleBatchConfirm`, `handleBatchReject`, `handleBatchStatusChange` use atomic `.update().in('id', ids)`.

### RAG Keywords
bulk actions, batch confirm, batch reject, bulk delete, calendar toolbar, selection mode, slot type filter

---

## Meeting Link Dynamic Label
### Problem
Label always showed "Default Meeting Link" regardless of mode.

### Edooqoo Solution
Label dynamically shows "Meeting Link" (no GCal), "Default Meeting Link" (default mode), or "Custom Meeting Link" (custom mode).

### Technical Mechanics
- `src/pages/StudentPage.tsx`: conditional label based on `autoLinkEnabled` and `mode`.

### RAG Keywords
meeting link label, custom meeting link, default meeting link, student page

---

## Student Hub Email Display
### Problem
No indication which email is logged in on `/my/` pages.

### Edooqoo Solution
Email shown next to "Log out" button in `StudentHubLayout` header (hidden on mobile).

### Technical Mechanics
- `src/components/student-hub/StudentHubLayout.tsx`: `getSavedHubEmail()` displayed in `<span>`.

### RAG Keywords
student hub, logged in email, hub layout, student identity
